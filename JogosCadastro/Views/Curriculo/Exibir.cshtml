@model CurriculoViewModel
@{
    Layout = null;
}


<html>
<head>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">

    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Holtwood+One+SC&family=Roboto:wght@700&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a42a254279.js" crossorigin="anonymous"></script>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Indie+Flower&family=Permanent+Marker&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/Exibir.css">

    <script type="text/javascript" src="~/js/Exibir.js"></script>
</head>


<div class="curriculo-container">
    <!-- style=" "-->
    <div class="quadro-esquerdo">
        <div class="foto-container">
            <img src="data:image/jpeg;base64,@Model.ImagemEmBase64" class="ImagemRedonda" />
        </div>
        <section class="nome-container">
            <h1>@Model.Nome</h1>

        </section>
        <section class="dados-pessoais-container">
            <div class="dados-pessoais-items">
                <p><i class="fas fa-id-card"></i> @Model.CPF</p>
            </div>

            <div class="dados-pessoais-items">
                <p><i class="fas fa-envelope"></i> @Model.Telefone</p>
            </div>

            <div class="dados-pessoais-items">
                <p><i class="fas fa-envelope"></i> @Model.Email</p>
            </div>

            <div class="dados-pessoais-items">
                <p><i class="fas fa-home"></i> @Model.Rua nº: @Model.Numero_Endereco, @Model.Bairro @Model.Cidade - @Model.Estado</p>
            </div>

         </section>


        <section class="cargo-container">
            @if (ViewBag.Idioma == "I")
            {
                <p>Intended Position:</p>
            }
            else
            {
                <p>Cargo Pretendido:</p>
            }
            <h3>@Model.Cargo_Pretendido</h3>
        </section>

        <section class="midias-sociais-container">
            <div class="social-item">
                <i class="fab fa-facebook fa-2x"></i>
                <p>@Model.Facebook</p>
            </div>

            <div class="social-item">
                <i class="fab fa-linkedin fa-2x"></i>
                <p>@Model.Linkdin</p>
            </div>

            <div class="social-item">
                <i class="fab fa-instagram-square fa-2x"></i>
                <p>@Model.Instagram</p>
            </div>
        </section>

    </div>
    <!-- style="margin-left: 20px; width: 55%; float: left; -->
    <div class="quadro-direito">
        <div class="SobreMIM">
            @if (ViewBag.Idioma == "I")
            {
                <h2>About me</h2>
            }
            else
            {
                <h2>Sobre mim</h2>
            }
            <p>
                @Model.SobreMim
            </p>
        </div>
        <div class="Formacoes">
            @if (ViewBag.Idioma == "I")
            {
                <h2>Formations</h2>
            }
            else
            {
                <h2>Formações</h2>
            }

            @foreach (FormacaoViewModel f in Model.Formacao)
            {

                <div class="formacao item">
                    <h3>@f.Descricao</h3>

                    @if (ViewBag.Idioma == "I")
                    {
                        <p><strong>Instituition:</strong> @f.Instituicao</p>
                        <p><strong>Start:</strong> @f.Inicio.ToString("dd/MM/yyyy")</p>
                        <p><strong>End:</strong> @f.Fim.ToString("dd/MM/yyyy")</p>
                    }
                    else
                    {
                        <p><strong>Instituição:</strong> @f.Instituicao</p>
                        <p><strong>Inicio:</strong> @f.Inicio.ToString("dd/MM/yyyy")</p>
                        <p><strong>Fim:</strong> @f.Fim.ToString("dd/MM/yyyy")</p>
                    }


                </div>
            }
        </div>

        <div class="Habilidades">
            @if (ViewBag.Idioma == "I")
            {
                <h2>Skills</h2>
            }
            else
            {
                <h2>Habilidades</h2>
            }
            @foreach (HabilidadesViewModel h in Model.Habilidades)
            {
        <div class="habilidade item">
            <h3>@h.Descricao</h3>

            @if (ViewBag.Idioma == "I")
            {
                @if (h.Nivel == 0)
                {
                    <p><strong>Level:</strong> Iniciante</p>
                }
                else if (h.Nivel == 1)
                {
                    <p><strong>Level:</strong> Intermediario</p>
                }
                else
                {
                    <p><strong>Level:</strong> Avançado</p>
                }
            }
            else
            {
                @if (h.Nivel == 0)
                {
                    <p><strong>Nivel:</strong> Iniciante</p>
                }
                else if (h.Nivel == 1)
                {
                    <p><strong>Nivel:</strong> Intermediario</p>
                }
                else
                {
                    <p><strong>Nivel:</strong> Avançado</p>
                }
            }


        </div>
            }
        </div>


        <div class="Idiomas">

            @if (ViewBag.Idioma == "I")
            {
                <h2>Languages</h2>
            }
            else
            {
                <h2>Idiomas</h2>
            }

            @foreach (IdiomaViewModel i in Model.Idiomas)
            {
                <div class="Idiomas item">
                    <h3>@i.Idioma</h3>

                    @if (ViewBag.Idioma == "I")
                    {
                        <p><strong>Level:</strong> @i.Nivel</p>
                    }
                    else
                    {
                        <p><strong>Nível:</strong> @i.Nivel</p>
                    }
                </div>
            }


        </div>

    </div>
</div>


</html>

